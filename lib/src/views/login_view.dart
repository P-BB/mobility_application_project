import 'package:flutter/material.dart';

import 'package:mobility_application_project/src/constants/constants.dart';

class LoginView extends StatelessWidget {
  const LoginView({
    super.key,
  });

  static const routeName = RouteConstants.loginRoute;

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TODO 1'),
      ),
    );
  }
}
