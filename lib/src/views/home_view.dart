import 'package:flutter/material.dart';

import 'package:mobility_application_project/src/constants/constants.dart';


class HomeView extends StatelessWidget {
  const HomeView({super.key});

  static const routeName = RouteConstants.homeRoute;

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TODO 2'),
      ),
    );
  }
}
